This folder is for shared game-specific or software-specific NexPad config files.

The default config file includes the base controller bindings and keyboard or mouse mapping format. Use it as a reference when creating custom presets, and add generally useful configs here if you want to share them with other users.
